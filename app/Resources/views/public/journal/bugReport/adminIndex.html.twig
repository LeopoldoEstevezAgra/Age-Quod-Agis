{% extends 'public/journal/base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}

    <style>
table, th, td {
    border: 1px solid black;
    border-collapse: collapse;
}
th, td {
    padding: 15px;
}
    </style>
{% endblock %}

{% block body %}
    <table class="width-100" >
        <tr>
            <th>Title</th> 
            <th>Screen</th> 
            <th>Description</th>
            <th>Img</th>
        </tr>

        {% for bug in bugs %}
            <tr>
                <td>
                    {{ bug.title }}
                </td>
                <td>
                    {{ bug.screen }}
                </td>
                <td>
                    {{ bug.description }}
                </td>
                <td class="text-center" style="width: 10px;">
                    {% if bug.imageName %}
                        <img style="" src="{{ vich_uploader_asset(bug, 'imageFile') }}" alt="" />
                    {% endif %}
                </td>
            </tr>
        {% endfor %}
    </table>

{% endblock %}

{% block javascripts %}
    {{ parent() }}

{% endblock %}
